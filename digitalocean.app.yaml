name: polleneer-app
region: nyc
services:
- name: backend
  github:
    branch: main
    deploy_on_push: true
  source_dir: backend
  build_command: npm install
  run_command: npm start
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
